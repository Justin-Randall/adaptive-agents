---
description: Triage and recommend a disposition for a retrospective note
---

Read the latest retrospective notes from `<REPO_ROOT>/retrospectives/inbox/`. For any note whose status is still "Captured", triage it:

1. Determine if the lesson is cross-project (User-wide) or project-specific (Project Layer).
2. Evaluate if it should be Promoted, Deferred, or Rejected.
3. If promoting, specify the target scope path (e.g., `instructions/`, `memory/`, `skills/`).
4. Summarize your recommendation but do not make any changes without approval.
