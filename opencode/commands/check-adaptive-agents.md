---
description: Verify whether Adaptive Agents guidance is active and loaded
---

Check whether this OpenCode session has loaded the Adaptive Agents user-wide guidance from the canonical repository. Confirm by verifying that `<REPO_ROOT>/AGENTS.md`, `<REPO_ROOT>/INDEX.md`, and `<REPO_ROOT>/instructions/global.instructions.md` are accessible and have been applied as instructions.

If Adaptive Agents is active, respond with:
ADAPTIVE_AGENTS_GLOBAL_LOADED

Then summarize the key rules that are available: boundary rules, coding standards, retrospective process, and any active plan context.
