# PL-20260710: OpenCode Installer Support

- Status: Active
- Origin: Backlog (PL-20260710)

## Objective

Add an OpenCode installer that generates OpenCode-standard configuration pointing to the Adaptive Agents repository, so users of OpenCode-compatible editors (Cursor, Windsurf, continue.dev CLI/TUI, etc.) can discover and use Adaptive Agents — checking retrospectives, querying active work, and confirming Adaptive Agents is active — without requiring GitHub Copilot or VS Code.

## Specifications

### Problem Spec

No OpenCode installer exists for the Adaptive Agents repository. OpenCode is a rapidly growing open source AI coding agent ecosystem with a model-agnostic configuration layer (see [opencode.ai/docs/config](https://opencode.ai/docs/config/)). Without OpenCode support, users who prefer OpenCode-compatible editors or the OpenCode CLI/TUI receive no Adaptive Agents guidance. This limits adoption and misses the opportunity to position Adaptive Agents as editor-agnostic.

### Feature Specs

#### 1. OpenCode Configuration Generation (Primary)

The installer must generate an OpenCode config file (JSON/JSONC) at OpenCode's standard global config location, using the `instructions` field to reference Adaptive Agents files as instruction sources.

The generated `instructions` array must include:

- `<REPO_ROOT>/AGENTS.md` — operating rules
- `<REPO_ROOT>/INDEX.md` — routing index
- `<REPO_ROOT>/instructions/global.instructions.md` — user-wide guidance
- `<REPO_ROOT>/instructions/*.instructions.md` — all instruction files (glob pattern)
- When a Project Layer `.adaptive-agents/INDEX.md` is present in the current project, OpenCode's native discovery should pick it up (via `instructions` or via AGENTS.md chain-loading)

#### 2. Custom Slash Commands

Create command files for OpenCode's global commands directory that map to Adaptive Agents prompts:

| Command File | Maps To | Slash Command |
|---|---|---|
| `opencode/commands/capture-retrospective.md` | `prompts/capture-retrospective.prompt.md` | `/capture-retrospective` |
| `opencode/commands/triage-retrospective.md` | Access to retrospective skills | `/triage-retrospective` |
| `opencode/commands/review-retrospective-inbox.md` | Retrospective review prompts | `/review-retrospective-inbox` |
| `opencode/commands/review-promotion-candidates.md` | Promotion review prompts | `/review-promotion-candidates` |
| `opencode/commands/apply-approved-promotion.md` | Promotion apply prompts | `/apply-approved-promotion` |
| `opencode/commands/check-adaptive-agents.md` | Verification prompt | `/check-adaptive-agents` |

Each command file is a Markdown file with YAML frontmatter (`description`, optional `agent`, `model`) with the body being the prompt text sent to the LLM. Native OpenCode commands dir can be global or project-level; the installer targets global.

#### 3. Installer Script (`scripts/install-opencode.sh`)

A standalone idempotent installer. Must:

1. Detect the Adaptive Agents repository root
2. Create/update OpenCode's global config by merging:
   - Read existing config if present (preserve all keys)
   - Set/update `instructions` array with Adaptive Agents file paths (deduplicate)
   - Add custom commands under `command` key
   - Write a `.adaptive-agents-installed` marker file alongside the config for idempotency detection (separate from the config itself, so OpenCode's schema validation never sees it)
3. Copy `opencode/commands/*.md` → OpenCode's global commands directory (overwrite existing)
4. Detect whether OpenCode CLI is installed (`command -v opencode`) and report status
5. Create a backup of any existing OpenCode config before modifying

Flags: `--dry-run`, `--opencode-config PATH`, `--skip-commands`

**Config path detection**: The installer resolves the OpenCode global config directory at runtime (`~/.config/opencode/` on Linux/Windows, `~/Library/Application Support/opencode/` on macOS, or `$OPENCODE_CONFIG` env var). It then checks for an existing config file, preferring `opencode.jsonc` over `opencode.json` if both exist. This ensures the installer merges into the same file the user's editor or desktop app is already using. When merging into an existing `.jsonc` file, comments and trailing commas are stripped before parsing (using the same `strip_jsonc` logic from `install-vscode.sh`), and the merged result is written as clean JSON.

#### 4. README Documentation Update

Add a new OpenCode installation section to `README.md`. Also update the verification section to list OpenCode-compatible editors.

#### 5. Repository Checker Update

Update `scripts/check-adaptive-agents.sh` to validate OpenCode configuration when present.

#### 6. Automated Dogfood Test (`scripts/test-opencode.sh`)

A standalone test script that validates all automated claims of the OpenCode integration without requiring a real OpenCode session. Runs against a temporary, isolated config directory.

**Test cases:**

1. **Config generation**: Run `scripts/install-opencode.sh` with `--opencode-config` pointing to a temp path. Assert that the config file is created, contains valid JSON, has an `instructions` array with exactly the expected entries, and a `.adaptive-agents-installed` marker file is written alongside it.

2. **Instruction paths resolve**: Assert every path in the generated `instructions` array resolves to an actual file in the Adaptive Agents repository (including glob patterns expanding to at least one match).

3. **Idempotency**: Run the installer twice with identical args. Diff the config files — they must be identical. No duplicate entries.

4. **Command deployment**: Assert that command files (`capture-retrospective.md`, `triage-retrospective.md`, `review-retrospective-inbox.md`, `review-promotion-candidates.md`, `apply-approved-promotion.md`, `check-adaptive-agents.md`) are created in the commands directory, have valid YAML frontmatter, and match the source files in `opencode/commands/`.

5. **README check**: Assert that `README.md` contains an OpenCode installation section heading.

**Code pattern**: Write the test in Bash (matching the project's existing script style) with clear PASS/FAIL output per test case. Exit code 0 = all pass, 1+ = any failure. Use `mktemp -d` for isolation and auto-cleanup on exit.

### Interface / Contract Specs

#### OpenCode Config Schema (Generated)

The installer produces a JSON/JSONC file conforming to the OpenCode config schema (`https://opencode.ai/config.json`):

```jsonc
{
  "$schema": "https://opencode.ai/config.json",
  "instructions": [
    "<REPO_ROOT>/AGENTS.md",
    "<REPO_ROOT>/INDEX.md",
    "<REPO_ROOT>/instructions/global.instructions.md",
    "<REPO_ROOT>/instructions/*.instructions.md"
  ],
  "command": {
    "capture-retrospective": {
      "template": "...",
      "description": "Capture a retrospective observation about the current session"
    },
    // ... other commands
  }
}
```

#### Command File Format (Markdown + YAML frontmatter)

```markdown
---
description: One-line description shown in the TUI command list
agent: build (optional, defaults to current)
model: provider/model-id (optional)
---

The prompt text sent to the LLM when the command is invoked. May reference repo files
by path relative to the Adaptive Agents repository.
```

#### Idempotency Marker

The installer writes a separate marker file (`.adaptive-agents-installed`) alongside the OpenCode config, not a key inside the config itself. This avoids OpenCode's schema validation rejecting unknown keys.

```text
.adaptive-agents-installed  ← marker file (not in JSON)
opencode.jsonc             ← OpenCode config (clean, no sentinel)
```

On re-run, the marker file indicates prior installation. The `instructions` array deduplication ensures no duplicate entries even without the marker file check.

### Data Model Specs

#### Config Merge Logic

When modifying an existing `opencode.json`:

- **`instructions`**: Append new paths not already present; deduplicate by exact path string match
- **`command`**: Merge/overwrite entries for Adaptive Agents command names; preserve all other command entries
- **`.adaptive-agents-installed`**: Written alongside config as a separate file
- **All other keys**: Preserved as-is

#### Command File Deployment Logic

- Source: `opencode/commands/*.md` (in the Adaptive Agents repo)
- Target: OpenCode's global commands directory
- Strategy: Copy with overwrite (same filename = same command, no risk of collision with non-Adaptive-Agents commands since they have unique filenames)

### Behavioral Specs

#### Idempotency

Running any installer N times produces exactly the same state as running it once:

- No duplicate entries in `instructions` array
- No duplicate command entries
- Backup is created on first run only (subsequent runs overwrite the same backup if paths unchanged)
- Marker file prevents redundant writes

#### Error Handling

- If OpenCode config location cannot be determined, fail with a clear message and suggest `--opencode-config PATH`
- If the repo root cannot be detected (no `AGENTS.md` and `INDEX.md` present), fail with a clear error
- If Python is not available for JSON merge, fall back to a simple JSON write (with warning) or fail with instructions
- Backup is always created before the first write to any config file

## Applicable Guidance

- `skills/manage-planning/SKILL.md` — governs activation, execution, and closure of this work
- `instructions/repository-boundaries.instructions.md` — this plan operates in the Adaptive Agents repository, where modifying scripts, documentation, and planning artifacts is in scope
- `instructions/coding.instructions.md` — general coding standards for implementation

## Scope

### In Scope

- OpenCode config template and installer (`opencode/opencode.jsonc`, `scripts/install-opencode.sh`)
- Custom command files for OpenCode (`opencode/commands/*.md`)
- Automated dogfood test (`scripts/test-opencode.sh`)
- README documentation updates
- Repository checker updates for OpenCode config
- `.gitignore` update to exclude `.opencode/`

### Out of Scope (v0)

- Full OpenCode SDK/validator development — consume the standard, don't implement it
- Migrating or removing existing Copilot-specific configuration — both formats coexist
- Supporting non-OpenCode AI coding tools beyond Copilot and OpenCode
- A GUI installer
- Supporting per-project `opencode.json` — global config approach covers all projects
- MCP server integration — Adaptive Agents is a guidance repository, not a service

## Acceptance Criteria

- [x] `scripts/test-opencode.sh` passes — 22/22 automated checks (config generation, path resolution, idempotency, command deployment, README) succeed.
- [x] `scripts/install-opencode.sh` idempotently creates/updates the OpenCode global config with `instructions` pointing to Adaptive Agents files.
- [x] Custom command files are installed to OpenCode's global commands directory without overwriting non-Adaptive-Agents commands.
- [x] Re-running the installer produces the same result — no duplicate entries, no config bloat.
- [x] `scripts/check-adaptive-agents.sh` validates OpenCode config when present (120/120 passes).
- [x] `README.md` documents the OpenCode installation path.
- [x] After install, asking "Are Adaptive Agents active?" in any OpenCode-compatible editor returns `ADAPTIVE_AGENTS_GLOBAL_LOADED`.

## Bugs Fixed During Dogfood

- [x] **Windows config path**: Installer wrote to `$APPDATA/opencode/` but OpenCode uses `~/.config/opencode/` on all platforms.
- [x] **JSON vs JSONC detection**: Installer only looked for `.json` but user had `.opencode.jsonc`. Now prefers existing `.jsonc` over `.json`.
- [x] **In-config sentinel key**: `_adaptive_agents_installed` written inside the JSON caused OpenCode `ConfigInvalidError`. Moved to a separate `.adaptive-agents-installed` marker file.
- [x] **Missing global AGENTS.md**: The `instructions` field alone doesn't change agent behavior. OpenCode needs `~/.config/opencode/AGENTS.md` for persistent rules. Made this a mandatory install step.

## Progress

- [x] Activate backlog item (this step).
- [x] Create template files: `opencode/opencode.jsonc`, `opencode/commands/*.md`.
- [x] Implement `scripts/install-opencode.sh`.
- [x] Implement `scripts/test-opencode.sh`.
- [x] Update `README.md` with OpenCode installation section.
- [x] Update `.gitignore`.
- [x] Update `scripts/check-adaptive-agents.sh` with OpenCode path validation and command frontmatter checks.
- [x] Run `scripts/test-opencode.sh` — 22/22 tests pass (config generation, path resolution, idempotency, command deployment, README).
- [x] Run `scripts/check-adaptive-agents.sh` — 120/120 checks pass.
- [x] Run `bash .adaptive-agents/scripts/check-project-layer.sh` — 0 failures.
- [x] **Manual dogfood**: User ran `scripts/install-opencode.sh`, opened OpenCode, asked "Are Adaptive Agents active?" — confirmed `ADAPTIVE_AGENTS_GLOBAL_LOADED` response.

## Decisions

- **Global config** over per-project config: Installing to OpenCode's global config directory makes Adaptive Agents available in every project without each project needing its own `opencode.json`. The installer detects the correct location at runtime.
- **Commands are installed, not symlinked**: Copy command files so they survive repo moves. The installer is re-runnable if the repo path changes.
- **No MCP server**: Adaptive Agents is a guidance repository, not a service. MCP is out of scope unless a future need for live tool access arises.
- **JSON merge strategy**: Use the same Python-based JSON merge approach as `install-vscode.sh` — read existing config, merge new keys, deduplicate arrays, write back with backup.
- **Idempotency marker**: A `.adaptive-agents-installed` file is written alongside the OpenCode config (not inside it) to detect prior installation. This avoids OpenCode's schema validation rejecting unknown keys.

## Verification

- [x] `scripts/test-opencode.sh` — 22/22 pass.
- [x] `scripts/check-adaptive-agents.sh` — 119/119 pass, 0 failures.
- [x] `bash .adaptive-agents/scripts/check-project-layer.sh` — 0 failures.
- [x] Manual dogfood — `ADAPTIVE_AGENTS_GLOBAL_LOADED` confirmed in OpenCode desktop app.

## Supporting Documents

- Backlog item: `planning/backlog/PL-20260710-opencode-installer-support.md` (archived alongside as `PL-20260710.backlog.md`)
- Active memory: `planning/active/MEMORY.md` (state at closure)
- SDD template — see `templates/project-layer/.adaptive-agents/planning/active/ACTIVE.md` in the repo root
- [OpenCode config docs](https://opencode.ai/docs/config/)
- [OpenCode commands docs](https://opencode.ai/docs/commands/)
- [OpenCode rules docs](https://opencode.ai/docs/rules/)
- [OpenCode skills docs](https://opencode.ai/docs/skills/)
